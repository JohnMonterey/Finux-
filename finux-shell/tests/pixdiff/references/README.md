# Reference captures

Intentionally empty. See `docs/ASSETS.md`.

Screenshots of the Windows 10 UI belong to Microsoft and are not redistributed
here. Capture your own at 100% scaling and save them in this directory as
`<test-name>.png`; each pixdiff test names the file it wants and writes what it
actually rendered to `tests/pixdiff/output/` for comparison.
